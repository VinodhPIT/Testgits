import 'package:flutter/material.dart';

class Mealsdetail extends StatelessWidget {
  const Mealsdetail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Detail')),
      body: Container(
        child: Center(
          child: Text("hai"),
        ),
      ),
    );
  }
}
